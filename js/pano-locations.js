/* ============================================================
   Capture locations for the 360° panoramas.
   Hand-edited — keyed by the image filename exactly as it appears
   in photos_360/ (case-sensitive, including the extension).

   The little map panel in the 360° viewer appears ONLY for images
   listed here. Remove an entry (or leave it out) to hide the map for
   that photo. Replace the sample coordinates below with each photo's
   real location — the panoramas carry no GPS data, so this is manual.

     lat / lng : decimal degrees (e.g. from Google Maps: right-click → coords)
     label     : caption shown under the map
     zoom      : optional initial zoom (default 13)
   ============================================================ */
window.PANO_LOCATIONS = {
  "R0011197.JPG": { lat: 6.9271, lng: 79.8612, label: "Colombo, Sri Lanka (sample — replace)", zoom: 13 },
  "R0011293.JPG": { lat: 7.2906, lng: 80.6337, label: "Kandy, Sri Lanka (sample — replace)", zoom: 13 }
};
