# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini

# Claude Code workspace
.claude/

# Build cache — regenerated by generate_manifest.py (machine-local mtimes)
.media-cache.json

# Local-only helpers (the hosted site replaces these)
Open Website (360 enabled).bat
READ ME FIRST - opening 360.txt
plan-next.txt
Information.txt

# Logs and archives (kept out of the deploy)
*.log
*.zip
